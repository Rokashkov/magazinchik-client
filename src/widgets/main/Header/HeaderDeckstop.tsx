import styles from './HeaderDeckstop.module.sass'
import cn from 'classnames'

export const HeaderDeckstop = () => {
	
	return (
		<header className={ cn(styles.header) }>Header Deckstop</header>
	)
}